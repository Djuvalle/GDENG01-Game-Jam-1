namespace GameEnum
{
    public enum ActionEvent
    {
        IngredientAdded,
    }
    public enum ParameterKey
    {
        IngredientType,
    }
    public enum IngredientType
    {
        Batter,
        Butter
    }

    public enum ActionState
    {
        Success,
        Failure
    }
}